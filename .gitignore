.idea/
*.iml
target/
*.class
.DS_Store
.vite/
.vite/deps/
node_modules/
.env
dist/
build/
# Rapports de test (énormément de HTML généré)
coverage/
# Build Electron local (centaines de Mo, pas versionné)
client/dist-electron/
.DS_Storetsconfig.tsbuildinfo
server/tsconfig.tsbuildinfo
server/junit.xml
server/junit.xml
.cursor/**
/Docs/SSH/
temporaire/
# Journaux de sauvegardes locales (cron / launchd)
backups/*.log

# Game_Versions/ : uniquement installables (.exe, .dmg, .apk, .ipa, .zip d’archive…)
Game_Versions/*.txt
Game_Versions/*.md
# Copie locale avec ton Team ID (voir ExportOptions-*.plist.example)
scripts/ios/ExportOptions.plist
