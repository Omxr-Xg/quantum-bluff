/**
 * =============================================================================
 * EXAMPLE.TEST.TS — Test exemple Jest (backend)
 * =============================================================================
 *
 * Exemple de structure de test pour le projet. À conserver ou remplacer
 * par des tests métier selon les besoins.
 *
 * =============================================================================
 */

describe('Example test', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
});