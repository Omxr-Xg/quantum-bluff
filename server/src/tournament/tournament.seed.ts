/** Seed 32-bit déterministe depuis l’UUID tournoi (reproductible). */
export function seedFromTournamentId(id: string): number {
  let h = 2166136261 >>> 0
  for (let i = 0; i < id.length; i++) {
    h ^= id.charCodeAt(i)
    h = Math.imul(h, 16777619)
  }
  return h >>> 0
}
