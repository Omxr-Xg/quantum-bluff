/**
 * =============================================================================
 * EXAMPLE.TEST.TSX — Test exemple Vitest (frontend)
 * =============================================================================
 *
 * Exemple de structure de test pour le client. À conserver ou remplacer
 * par des tests métier (composants, hooks, utils).
 *
 * =============================================================================
 */

import { describe, it, expect } from 'vitest';

describe('Example test', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
});