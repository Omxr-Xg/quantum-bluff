<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Thème sombre : mêmes tons que l’UI web (plus de Theme.Light → barres blanches). -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/app_window_bg</item>
        <item name="android:statusBarColor">@color/app_window_bg</item>
        <item name="android:navigationBarColor">@color/app_window_bg</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/app_window_bg</item>
        <item name="android:statusBarColor">@color/app_window_bg</item>
        <item name="android:navigationBarColor">@color/app_window_bg</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/app_window_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <!-- Rétrocompat / Capacitor : image plein écran si besoin -->
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>
