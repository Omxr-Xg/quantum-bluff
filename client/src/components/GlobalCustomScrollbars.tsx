/**
 * Ancien overlay de scroll « jeu » — supprimé au profit de la barre native.
 * Composant conservé vide pour éviter les ReferenceError si une vieille référence ou un cache bundler subsiste.
 */
export function GlobalCustomScrollbars() {
  return null;
}
